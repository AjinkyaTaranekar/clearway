server:
  port: 8085
  timeout: 30s

database:
  master:
    host: "localhost"
    port: 5432
    user: "postgres"
    password: ""
    dbname: "trafficservice"
    max_open_conns: 25
    max_idle_conns: 5
  slave:
    host: "localhost"
    port: 5432
    user: "postgres"
    password: ""
    dbname: "trafficservice"
    max_open_conns: 50
    max_idle_conns: 10

logging:
  level: "info"
  format: "console"
